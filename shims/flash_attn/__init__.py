"""Windows FA4 shim package.

This package intentionally shadows the experimental upstream `flash_attn.cute`
entrypoint when `shims/` is placed at the front of `sys.path`.
"""

__version__ = "0.0.0-windows-shim"

